TOKEN EXPIRATION HANDLING STATUS:

The fetchWithAuth wrapper function is already defined at the top of AdminDashboard.jsx (lines 7-20).
It checks for 401 status and redirects to '/' (login page).

However, ALL fetch() calls throughout the file need to be replaced with fetchWithAuth().

Current status: fetchWithAuth is defined but NOT being used in any of the actual API calls.

To complete the implementation:
- Replace all `fetch(` with `fetchWithAuth(`
- Remove `credentials: 'include'` from options (fetchWithAuth adds it automatically)

This will ensure that when the JWT token expires, users are automatically redirected to the login page.
